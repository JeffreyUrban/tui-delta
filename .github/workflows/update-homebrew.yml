name: Update Homebrew Formula

permissions:
  contents: write
  issues: write

on:
  release:
    types: [published]

jobs:
  trigger-homebrew-update:
    runs-on: ubuntu-latest
    steps:
      - name: Extract version from tag
        id: version
        run: |
          VERSION=${GITHUB_REF#refs/tags/v}
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Triggering Homebrew formula update for version $VERSION"

      - name: Trigger Homebrew tap update
        uses: peter-evans/repository-dispatch@v3
        with:
          token: {% raw %}${{ secrets.HOMEBREW_UPDATE_TOKEN }}{% endraw %}
          repository: JeffreyUrban/homebrew-tui-delta
          event-type: update-formula
          client-payload: '{"version": "{% raw %}${{ steps.version.outputs.version }}{% endraw %}"}'
